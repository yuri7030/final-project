package sorting

import (
    "sort"
)

func SortInts(arr []int) {
    sort.Ints(arr)
}

func SortFloats(arr []float64) {
    sort.Float64s(arr)
}

func SortStrings(arr []string) {
    sort.Strings(arr)
}
